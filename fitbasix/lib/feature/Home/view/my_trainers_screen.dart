import 'package:flutter/material.dart';

class MyTrainersScreen extends StatelessWidget {
  const MyTrainersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('My Trainers Screen'),
      ),
    );
  }
}
